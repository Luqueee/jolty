# Milestone 13 PEFT experiment protocol

Status: the v9 corpus, fitting recipe, and model-selection rule were fixed before fitting. The matched frozen head and LoRA candidate have been fitted on train and calibrated on validation. No v9 test prediction has been scored, and no runtime provider exists yet. The published browser-adapted Laya collection seed list includes all three v9 test origins; actual training exposure is unknown. The first [blind comparison](m13-blind-comparison.md) was scored once under the compact input. A [second small diagnostic set](m13-value-aware-followup.md) was opened under a value-aware input; its v10s scoring run aborted before persisting results. Neither result supports a fast-path change, and the larger deployment evaluation described below remains open.

## Evaluation freeze and browser-checkpoint site audit

This is the record for deciding whether Laya should replace MiniLM in Jolty's fast path. Keep the 40 v9 test decisions and nine three-step flows sealed until input format, model roster, calibration rule, and thresholds are fixed from training and validation only. The frozen-head and MiniLM LoRA artifacts already exist; a comparison with the published browser Laya must first resolve its site contamination. Do not use test labels, live flow outcomes, or per-site test errors to choose a prompt, candidate order, checkpoint, temperature, threshold, or retraining recipe. Run all models on the same saved observation and retrieved Top-10 for step comparisons. Use the same fresh page and deterministic postcondition for live flows; keep the teacher outside browser execution. Report retrieval omissions separately from model choices, and report abstention coverage with covered correctness and high-confidence errors per site. Do not interpret 40 decisions as evidence of 99% accepted-decision precision.

Frozen local inputs on 2026-09-24 (the corpus's embedded content digest differs from its whole-file SHA-256):

| Input | Identifier |
| --- | --- |
| Repository code before this protocol clarification | Git `7b1914ad67012450ab1ed6189a6c7ec73d295cd8` |
| v9 corpus | Embedded content SHA-256 `067e957e44c7d33e39f03527da2b754323ff31429d52e164921b08e9ad40914c`; whole-file SHA-256 `4f583f9d8b1b0b0155d93aa2d3dc91416430e0ff0c0fc3f24bce3cb20ca60030` |
| Original Laya in Jolty | `receptron/laya-onnx` revision `68f27dfe5a27a54fb2b1fefc432f43f972e90868`, through `@receptron/laya@0.1.2` |
| Browser-adapted Laya candidate | `cklxx/laya-browser` revision `4219958196e2c566c141688c773e08da10c1ff3b`; use `v10s` as the initial published candidate, with its recorded input format; `v10` is a distinct checkpoint and must be declared before comparison if included |
| MiniLM encoder | `sentence-transformers/all-MiniLM-L6-v2` revision `1110a243fdf4706b3f48f1d95db1a4f5529b4d41` |
| Matched frozen head | `artifacts/frozen-encoder-v9.json`, whole-file SHA-256 `c4d892f8b8c0fd259809422390545abe7d10e3b5d283161d231ab8d5b43b2dea` |
| MiniLM LoRA | `artifacts/peft-v9/adapter/adapter_model.safetensors` SHA-256 `e44120625aca37e009d93c76ff0c658c3219678818cfb9b956fd0a15428caf0b`; `head.json` SHA-256 `9a0edc8b38c5efd7ffe7f02eb87e739cbb9516c0a17be9d39ce` |
| Measurement host | AMD Ryzen 7 9700X, 16 logical CPUs, 30 GiB RAM, NVIDIA RTX 4060 Ti with 8 GiB VRAM; record Node, Python, runtime, browser, thread count, and power settings with each timed run |

The [published collection script at the pinned browser-checkpoint revision](https://huggingface.co/cklxx/laya-browser/blob/4219958196e2c566c141688c773e08da10c1ff3b/code/finetune/collect_pages.py) explicitly seeds `automationexercise.com`, `www.demoblaze.com`, and `parabank.parasoft.com`. These are exactly the v9 test origins. The [published v10s build command](https://huggingface.co/cklxx/laya-browser/blob/4219958196e2c566c141688c773e08da10c1ff3b/code/finetune/run_v10s.sh) combines collected pages, generated cases, executed DONE and step-2 cases, Mind2Web conversions, and DAgger corrections. Its [builder](https://huggingface.co/cklxx/laya-browser/blob/4219958196e2c566c141688c773e08da10c1ff3b/code/finetune/build_items.py) assigns generated pages by page index and Mind2Web examples by website hash, then writes tokenized training items without source URLs. The [pinned Hub tree](https://huggingface.co/cklxx/laya-browser/tree/4219958196e2c566c141688c773e08da10c1ff3b) has 122 entries and no `.jsonl`, `.parquet`, `.pt`, or manifest data files. Therefore the public artifact does not establish which seed URLs were successfully collected or which pages and corrections entered the final train split. The seed list proves **exposure risk**, not that each page or v9 decision was used for fitting. None of the three origins can currently support an unseen-site claim for this checkpoint. Keep v9 intact for the original predeclared MiniLM comparison and treat browser-Laya results there as exposure-uncertain diagnostics only.

The effective-data audit is closed at the verifiable public limit: the exact `pages.jsonl`, `cases.jsonl`, `done_cases.jsonl`, `step2_cases.jsonl`, `m2w_cases.jsonl`, and `dagger_cases.jsonl` used to build v10s are not published. If those files or a trustworthy derived manifest later become available, reconstruct the builder's partition, normalize training URLs to origin and template family, and compare exact URLs, origins, and sibling deployments. Until then, mark exposure **unknown** and continue with the separately authored blind test.

Before making a replacement decision using browser-adapted Laya, create a separate site-disjoint test set: choose new origins absent from Jolty's training, validation, historical test, and the effective browser-Laya training inventory; inspect sibling deployments and shared templates; reserve whole origins before writing labels. If a complete upstream inventory is unavailable, record that limitation and use newly authored local sites with independently designed templates, or sites demonstrably launched after checkpoint training. The [source inventory, import pilot, and candidate-site matrix](m13-data-pilot.md) record the current preparation. The new evaluation design requires at least six origins across at least three unrelated application/template families, at least 60 distinct decision states, and at least two flows of three or more steps per origin. Include field/button conflicts, near-duplicate labels, post-action states, native selections where supported, and task completion states. Count distinct states, not repeat runs or paraphrases. Reserve whole origins and template families before authoring labels; record case IDs, site/template groups, collection code revision, content digest, and collection date. Every label must have an independently checked action and target, a deterministic postcondition, and a recorded retrieval rank. Each reference flow must pass in three fresh contexts before admission. No model prediction may be inspected during authoring. The new set must not be used for threshold or prompt selection. This larger deployment set has **not yet been created**; the two opened diagnostic sets do not meet all these conditions, and the v9 test embargo remains in force.

The same frozen observations, retrieved Top-10, action vocabulary, planned values, and validators must be used for the heuristic, original Laya, browser-adapted Laya v10s, matched frozen MiniLM head, and MiniLM LoRA v9. A model-specific serializer may express the common information in its required format, but its fields, token truncation, option order, and any unsupported action must be recorded before scoring. Keep every retrieval miss in the task denominator. Report `Recall@10` over target-bearing states and exact action-and-target accuracy conditional on target presence separately; report unconditional exact accuracy as well. Live task completion uses fresh contexts and identical starts, with model errors, retrieval misses, execution failures, validation failures, and site failures distinguished. Repeat each flow three times for execution variability without counting repeated states as independent cases.

**Predeclared replacement rule.** Use only non-test validation states to choose each model's temperature, abstention threshold, and any deterministic heuristic acceptance rule. Among rules with zero observed accepted errors on validation, choose the one with greatest coverage; if none covers a decision, the model abstains. Freeze these rules, serializers, candidate order, and model artifacts before either test is opened. On the new site-disjoint test, compare completed flows under these fixed rules first, then coverage of correct accepted decisions at the same observed-error ceiling of **zero accepted wrong decisions**. A wrong accepted decision fails the safety screen regardless of aggregate task count; zero observed errors is only a small-sample screen, not evidence of 99% precision. The <25 ms decision-pipeline target comes from `SPECS.md` section 51 and `docs/performance.md`; both call it an engineering goal. The 1 GiB incremental-RSS bound was introduced in this research protocol and is **provisional**, not a product requirement. Neither is currently an established pass/fail gate because all candidates have not been measured under one pipeline. Report both as comparison targets without excluding a model until paired measurements and a deployment budget are approved. For the paired run, use the pinned Ryzen 7 9700X host, a single warm process and resident model, one decision per batch, and separate CPU-only and RTX 4060 Ti GPU-assisted modes. Time serialization, tokenization, inference, and postprocessing from the same start and stop points; report extraction, filtering, retrieval, browser navigation, model loading, and validation separately. Measure peak process RSS above the warmed no-model process under the same browser/workload, and peak device VRAM above the warmed device baseline. Record runtime versions, thread settings, power mode, warmup count, sample count, and p50/p95/p99. A passing safety and task screen still requires larger independent site-stratified trials, a justified resource budget, and ONNX parity before claiming high accepted-decision precision or replacing the runtime. The current Milestone 13 comparison gate remains open.

## Question and scope

Test whether adapting the pinned `sentence-transformers/all-MiniLM-L6-v2` encoder with LoRA improves Jolty's next-action decisions over a frozen encoder trained on the **same** new corpus. The v8 head beat Laya on ten new one-step cases but tied the fixed v7 head on those observations. It also repeated the catalog search-field error in all three measured multistep runs and the historical `learn-create-shadow` error at confidence 0.998746. More easy one-step labels cannot answer whether encoder adaptation fixes these problems.

This experiment is limited to `click`, `type`, and native `select` decisions whose targets appear in the main-document Top-10. Shadow-root and frame targets, drag-and-drop, and target-free actions require separate browser-state or action contracts; they must be recorded as retrieval or capability failures, not treated as encoder errors. The small model remains outside Jolty's normal browser path until a later deployment decision.

## Site and data allocation

The existing v8 training and calibration origins retain their splits. All test origins from v0–v8 remain reserved. The six new origins below were smoke-checked with Chromium on 2026-09-24; a successful page load is not a validated decision label.

| Split | New origins | Intended hard cases |
| --- | --- | --- |
| Train | [Play QA](https://www.play-qa.com/), [UPEX Dojo](https://dojo.upexgalaxy.com/) | Product and autocomplete search fields beside action buttons; repeated form fields; dialog close versus reopen; custom and native controls; table actions after a state change. |
| Calibration | [Syntax](https://www.syntaxprojects.com/) | Ambiguous labels, form and widget choices, and changed page state. Use only main-document controls. |
| Untouched test | [Automation Exercise](https://automationexercise.com/), [DemoBlaze](https://www.demoblaze.com/), [ParaBank](https://parabank.parasoft.com/parabank/index.htm) | Search and product navigation, cart or modal choices, and banking-form navigation without creating accounts or making transactions. |

`sources.ts` reserves these origins now. Do not move any evaluated test origin into train or calibration, including the v7/v8 sites and the two inspected multistep sites. Avoid sibling deployments of the same application across splits. If a new site becomes unusable before fitting, replace its **entire origin** and record the replacement and reason before scoring any test prediction.

GlobalSQA was replaced before label authoring or model scoring on 2026-09-24: its practice widgets are embedded in frames, outside this experiment's main-document target contract. Syntax offers main-document fields, tables, filters, and native selects. One Chromium observation found 1,103 extracted elements and 80.24 ms extraction on Automation Exercise's product page. This is a single cold observation, not a latency distribution. Report extraction separately from decision latency on this site; do not attribute total-loop time to the model. Its size is a useful stress case for the existing compact-state contract and may require a separately measured browser-state change before deployment.

The next corpus must contain at least 60 **new**, independently validated hard training decisions across the two new training origins, with at least 25 from each, at least 20 new calibration decisions on Syntax, and at least 30 distinct held-out decision states across the three new test origins, with at least ten per test origin. Author at least three multistep flows of three or more steps per test origin. Flow steps may contribute to the 30 decision states, but repeated runs and paraphrases of one state never increase the distinct-label count. Include at least six cases each for search-field/button conflict, duplicate or nearby labels, and post-transition controls across train and calibration. Keep click, type, and select represented in train and calibration; test action coverage depends on what the sites support. Do not manufacture an action merely to satisfy a count.

Each label needs a stable starting state, exact target and action, planned value when applicable, target Recall@10, executor success, deterministic validator result, and an independent postcondition. Run each newly authored flow's curated reference in three fresh browser contexts before admitting it. A source page that changes, requires credentials, or fails the privacy guard is excluded or repaired before any model score is read. Save a corpus digest, split groups, action counts, failure-mode tags, and the exact case list. Never promote a previously inspected test result into fitting or threshold selection.

## Fixed model comparison

Both candidates use the same validated training observations and Top-10 options. First fit the existing 388-feature frozen head on the new train split with the v8 zero-action-intercept recipe; use the new calibration split for its temperature and threshold. This is the matched baseline. Then adapt the **same pinned encoder revision** with LoRA on attention `query` and `value` projections (rank 8, alpha 16, dropout 0.05), mean pooling and unit normalization, and the same listwise candidate scoring contract. Initialize the 388-feature head from the matched frozen baseline and train the adapter and head together with listwise cross-entropy, keeping the three global action-intercept weights fixed at zero. Use maximum sequence length 128, four epochs, AdamW learning rate `2e-4`, weight decay 0.01, one decision group per microbatch, eight gradient-accumulation steps, and a fixed seed of 17. Do not tune these values on test results. Before fitting, compare the Python and TypeScript feature construction on 20 saved training/calibration observations: action options and top choices must match exactly, and each normalized embedding pair must have cosine similarity at least 0.999. Stop on a parity failure.

Training belongs in a separate Python process launched by `pnpm research`; no Python runtime enters `packages/browser`, `packages/decision`, or the normal CLI execution path. Use the local 8 GB NVIDIA GPU only after a one-batch memory smoke test. Record peak VRAM, elapsed training time, dependency versions, model revision, corpus digest, optimizer settings, adapter weights, and head weights. The repository currently has no Python training environment; add the minimal pinned training dependencies only when the new corpus passes admission. The official [PEFT LoRA configuration](https://huggingface.co/docs/peft/en/package_reference/lora) supports explicit projection targets and rank, while [PyTorch AMP](https://docs.pytorch.org/docs/stable/accelerator/amp.html) provides mixed-precision training if the memory smoke test requires it.

Choose the calibration temperature and abstention threshold without test labels. Score the untouched test once after the corpus, fitting recipe, and model-selection rule are frozen. Run the matched frozen head, PEFT candidate, Laya, and subscription-backed `gpt-6-sol` teacher on the same saved decision states; run the executable policies on the same live flows. The teacher scores saved observations only. Report per-origin and per-failure-mode exact action-and-target accuracy, task completion, target Recall@10, covered correct/incorrect decisions, confidence buckets and Brier score, p50/p95 decision latency, peak RSS/VRAM, and throughput. Distinguish model errors from retrieval, execution, validation, and site failures.

The engineering promotion gate requires PEFT to gain at least five exact decisions among the 30 held-out states **and** two completed flows among the nine minimum flows over the matched frozen baseline, exceed Laya in held-out task completion, and make no wrong decision at confidence 0.9 or above. It must also select the catalog search field correctly and either correct the historical `learn-create-shadow` decision or abstain below the calibration threshold; these historical cases are regression checks, not part of the untouched gain count. A passing result still needs ONNX export and measured Node inference before runtime integration. If the gate fails, record the negative result and stop rather than changing the adapter or case list after seeing test outcomes. Thirty states on three sites are an engineering gate, not a population-level accuracy claim.

## Fitting run before held-out scoring

Use `pnpm research peft --stage setup` to create the ignored Python environment and install the pinned top-level dependencies in `research/peft/requirements.txt`. Run `pnpm research train --version 9 --bias zero` for the matched frozen head, then `pnpm research peft --stage parity`, `pnpm research peft --stage smoke`, and `pnpm research peft --stage train`. The last three commands verify the same 20 saved train/validation states against the pinned TypeScript encoder before touching the GPU. The training input contains only train and validation samples; both the frozen-head report and PEFT report omit the test split. Python remains in a separate research process and its artifacts are ignored under `artifacts/`.

The 2026-09-24 fitting run used corpus digest `067e957e44c7d33e39f03527da2b754323ff31429d52e164921b08e9ad40914c`. The existing action contract excluded three UPEX custom-select training labels from **both** candidates; 168 training and 76 validation states entered fitting and calibration. No validation or test label was excluded. Parity compared 96 normalized embeddings across 20 states spanning both new training origins and Syntax: minimum cosine 0.99999988, with identical action options and top choices. The one-batch GPU smoke allocated at most 122,979,840 bytes on an 8 GB RTX 4060 Ti. Four LoRA epochs took 3.33 seconds and peaked at 165,925,888 allocated GPU bytes. These are PyTorch allocation measurements, not total device occupancy or inference benchmarks.

| Validation measure | Matched frozen head | LoRA candidate |
| --- | ---: | ---: |
| Exact decisions | 68/76 | 69/76 |
| Correct decisions covered by the validation-selected threshold | 41/41 | 42/42 |
| Temperature | 0.25 | 0.25 |

The LoRA adapter, 388-weight head with zero action intercepts, calibration, dependency versions, revision, digest, and recipe are saved in ignored `artifacts/peft-v9/`. The small validation difference does not establish generalization or promotion. Next, compare the declared models on validation observations without fitting more weights, check serialization and Top-10 coverage, and freeze the selection rule. The v9 one-time test comparison may then evaluate the predeclared MiniLM experiment; the browser-adapted Laya needs the separate site-disjoint test set described above for an unseen-site deployment decision.
